Para La importación debe de seguir el siguiente orden, estos son necesarios para el correcto funcionamiento del software:

	1)byytablas.sql
	2)usuarios.sql
	3)cargadedatosesenciales.sql

También puede importar el archivo "datosdeprueba.sql" el cual contiene datos de prueba
para poder probar el software, esto es solamente si desea probar el software con datos ficticios.

Gracias por leer!